[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_NAVER_MAPS_CLIENT_ID,NEXT_PUBLIC_API_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"
